---
title: 'Custom domain'
---

You can attach a custom domain name to an API served via Nadles API Gateway, so that, instead of the domain name assigned to your API by Nadles (like `https://<subdomain>.p.nadles.com/`), users access your API via the domain name owned by you.

